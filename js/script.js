let todos = [];

function addTodo() {
    const todoInput = document.getElementById('todo-input');
    const todoDate = todoInput.value.trim();

     /// Check if input is not empty
    if (todoInput) {}
}